# Unix_Shell
My Custom Shell is a Unix-like command-line shell written in C using Linux system calls. It supports built-in and external commands, file operations, output redirection (>), piping (|), and signal handling. The project demonstrates core system programming concepts such as process control, file descriptors, and inter-process communication.
